# Gemini API (required)
GEMINI_API_KEY=your_gemini_api_key_here

# MongoDB (required)
MONGODB_URI=mongodb://localhost:27017/multi-agent

# Redis (optional — falls back to in-memory if not set)
REDIS_URL=redis://localhost:6379

# Pinecone (optional — long-term vector memory)
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_INDEX=multi-agent-memory

# Brave Search API (optional — used by WebSearchTool)
BRAVE_SEARCH_API_KEY=your_brave_search_api_key_here

# Auth (set to true to disable auth checks in development)
AUTH_DISABLED=true
